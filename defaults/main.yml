---
# defaults file for ansible-role-portainer-manager
galaxy_info:
  author: Doc_Tiebeau
  description: Configure and manage Portainer
  company: Frogz-Group
  license: GNU GPL V3

  min_ansible_version: 2.10.12

  platforms:
    - name: Debian,Redhat,Alpine

  galaxy_tags:
    - portainer
    - docker

dependencies:
  - collection:
    - name: community.docker
      version: 1.7.0
      source: https://galaxy.ansible.com